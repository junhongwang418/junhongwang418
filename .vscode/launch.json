{
  "configurations": [
    {
      "name": "attach debugger to app",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:1234",
      "webRoot": "${workspaceRoot}/src"
    }
  ]
}
